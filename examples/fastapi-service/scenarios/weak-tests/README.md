# Weak Tests Scenario

Flawed change: the endpoint test asserts only that a response dictionary exists.
Corrected change: the integration test verifies the order id, status, and total.

Relevant sensor: `test-adequacy`.
