# Runtime Missing Scenario

Flawed completion claim: the order summary endpoint works, but no runtime log or
health check proves the request reached the domain layer. Corrected evidence is
shown in `.harness/logs/order-summary.log`.

Relevant sensor: `runtime-observability`.
