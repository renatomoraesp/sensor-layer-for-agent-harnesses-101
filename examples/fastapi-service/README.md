# FastAPI Service Example

This is a static FastAPI-style example. It is intentionally not a runnable
service dependency tree; it exists to show how architecture and runtime sensors
would inspect a web-service-shaped repository.
