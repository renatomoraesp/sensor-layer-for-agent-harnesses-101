# Architecture and Invariant Drift Check

Judge whether the change preserves documented layering and neighboring patterns.
Flag business logic in transport/UI layers, duplicate abstractions, and new
dependencies that conflict with the module's role.
