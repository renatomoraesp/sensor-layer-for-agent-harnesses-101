# Feature-State and WIP Boundary Check

Judge whether the diff stays inside the active feature boundary. Flag unrelated
files, speculative future work, dishonest feature-state updates, and work that
should be split into a separate change.
